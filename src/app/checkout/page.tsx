export default function Checkout() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-4">Checkout</h1>
      <p>Stripe checkout integration placeholder. In production, send the cart to a server route and create a Checkout Session.</p>
    </div>
  );
}
